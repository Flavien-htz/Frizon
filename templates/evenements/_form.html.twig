{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-success mb-1">{{ button_label|default('Enregistrer') }}</button>

<br>
<a class="btn btn-secondary mb-1" href="{{ path('evenements_index') }}">Retour</a>

{{ form_end(form) }}
