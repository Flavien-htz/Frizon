{% extends 'base.html.twig' %}

{% block title %}Modifier l'annonce
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier l'annonce</h1>

		{{ include('annonces/_form.html.twig', {'button_label': 'Enregistrer'}) }}

		{# <a class="btn btn-info" href="{{ path('annonces_index') }}">Retourner à la liste</a> #}

		{{ include('annonces/_delete_form.html.twig') }}
	</div>

{% endblock %}
